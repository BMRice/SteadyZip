<!-- Custom window chrome (docs/ui-design.md §3). Windows 10 LTSC has no rounded window corners
     and no Mica, so the chrome is hand-drawn: square window, 1px border, 32px title bar.
     Window-level drop shadows are intentionally omitted (they need AllowsTransparency, which
     costs hardware acceleration and ClearType); §2.5 shadows are not worth that trade here. -->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Colors.xaml" />
        <ResourceDictionary Source="Typography.xaml" />
        <ResourceDictionary Source="Icons.xaml" />
        <ResourceDictionary Source="Controls.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!-- 标题栏按钮：46x32、直角、图标取自 Tag -->
    <Style x:Key="WindowButton" TargetType="Button">
        <Setter Property="Width" Value="46" />
        <Setter Property="Height" Value="{StaticResource Size.TitleBarHeight}" />
        <Setter Property="Background" Value="{StaticResource Brush.Transparent}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Foreground" Value="{StaticResource Brush.TextSecondary}" />
        <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                        <Path x:Name="glyph" Width="16" Height="16" Stretch="None"
                              HorizontalAlignment="Center" VerticalAlignment="Center"
                              Fill="{StaticResource Brush.Transparent}"
                              Data="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}"
                              Stroke="{TemplateBinding Foreground}" StrokeThickness="1.3"
                              StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource Brush.HoverSurface}" />
                            <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource Brush.Border}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource Brush.TextDisabled}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 关闭按钮：悬停 Danger 底 + 白字 -->
    <Style x:Key="WindowCloseButton" TargetType="Button" BasedOn="{StaticResource WindowButton}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                        <Path x:Name="glyph" Width="16" Height="16" Stretch="None"
                              HorizontalAlignment="Center" VerticalAlignment="Center"
                              Fill="{StaticResource Brush.Transparent}"
                              Data="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}"
                              Stroke="{TemplateBinding Foreground}" StrokeThickness="1.3"
                              StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource Brush.Danger}" />
                            <Setter Property="Foreground" Value="{StaticResource Brush.OnAccent}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource Brush.DangerHover}" />
                            <Setter Property="Foreground" Value="{StaticResource Brush.OnAccent}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource Brush.TextDisabled}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 可调整大小窗口（主窗口） -->
    <Style x:Key="Window.App" TargetType="{x:Type Window}">
        <Setter Property="WindowStyle" Value="None" />
        <Setter Property="Background" Value="{StaticResource Brush.Background}" />
        <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
        <Setter Property="FontFamily" Value="{StaticResource Font.UI}" />
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Body}" />
        <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
        <Setter Property="TextOptions.TextRenderingMode" Value="ClearType" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="shell:WindowChrome.WindowChrome">
            <Setter.Value>
                <shell:WindowChrome CaptionHeight="32" ResizeBorderThickness="6" CornerRadius="0"
                                    GlassFrameThickness="0" UseAeroCaptionButtons="False" />
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Window}">
                    <Border x:Name="root" Background="{TemplateBinding Background}"
                            BorderBrush="{StaticResource Brush.Border}"
                            BorderThickness="{StaticResource Border.Thin}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="32" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0" Background="{StaticResource Brush.Background}">
                                <StackPanel Orientation="Horizontal" Margin="10,0,0,0" VerticalAlignment="Center">
                                    <Path Data="{StaticResource Icon.App}" Width="16" Height="16" Stretch="None"
                                          VerticalAlignment="Center" Fill="{StaticResource Brush.Transparent}"
                                          Stroke="{StaticResource Brush.Accent}" StrokeThickness="1.3"
                                          StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" />
                                    <TextBlock Text="{TemplateBinding Title}" Margin="8,0,0,0"
                                               VerticalAlignment="Center" FontSize="{StaticResource Font.Size.Caption}"
                                               Foreground="{StaticResource Brush.TextPrimary}"
                                               TextTrimming="CharacterEllipsis" />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                                            shell:WindowChrome.IsHitTestVisibleInChrome="True">
                                    <Button x:Name="MinButton" Style="{StaticResource WindowButton}"
                                            Tag="{StaticResource Icon.Minimize}" ToolTip="最小化"
                                            Command="{x:Static SystemCommands.MinimizeWindowCommand}" />
                                    <Button x:Name="MaxButton" Style="{StaticResource WindowButton}"
                                            Tag="{StaticResource Icon.Maximize}" ToolTip="最大化"
                                            Command="{x:Static SystemCommands.MaximizeWindowCommand}" />
                                    <Button x:Name="CloseButton" Style="{StaticResource WindowCloseButton}"
                                            Tag="{StaticResource Icon.Close}" ToolTip="关闭"
                                            Command="{x:Static SystemCommands.CloseWindowCommand}" />
                                </StackPanel>
                            </Grid>

                            <ContentPresenter Grid.Row="1" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- 子窗口：仅关闭按钮 -->
                        <Trigger Property="ResizeMode" Value="NoResize">
                            <Setter TargetName="MinButton" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="MaxButton" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="WindowState" Value="Maximized">
                            <Setter TargetName="MaxButton" Property="Tag" Value="{StaticResource Icon.Restore}" />
                            <Setter TargetName="MaxButton" Property="Command" Value="{x:Static SystemCommands.RestoreWindowCommand}" />
                            <Setter TargetName="MaxButton" Property="ToolTip" Value="向下还原" />
                            <Setter TargetName="root" Property="BorderThickness" Value="0" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 固定尺寸子窗口。WindowStartupLocation 不是 DependencyProperty（无法用 Setter 设置），
         因此由各子窗口自己在 XAML 上声明。 -->
    <Style x:Key="Window.Dialog" TargetType="{x:Type Window}" BasedOn="{StaticResource Window.App}">
        <Setter Property="ResizeMode" Value="NoResize" />
        <Setter Property="ShowInTaskbar" Value="False" />
        <Setter Property="shell:WindowChrome.WindowChrome">
            <Setter.Value>
                <shell:WindowChrome CaptionHeight="32" ResizeBorderThickness="0" CornerRadius="0"
                                    GlassFrameThickness="0" UseAeroCaptionButtons="False" />
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
